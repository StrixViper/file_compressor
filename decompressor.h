#ifndef DECOMPRESSOR_H
#define DECOMPRESSOR_H

// Decompresses the input file and writes decompressed data to the output file.
void decompressFile(const char* inputFile, const char* outputFile);

#endif
