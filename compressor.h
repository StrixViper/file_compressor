#ifndef COMPRESSOR_H
#define COMPRESSOR_H

// Compresses the input file and writes compressed data to the output file.
void compressFile(const char* inputFile, const char* outputFile);

#endif
